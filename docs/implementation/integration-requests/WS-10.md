# WS-10 Integration Requests

## APScheduler declaration

`requirements.txt` does not declare APScheduler, and the package is not installed in this worktree. Please add a compatible APScheduler declaration through WS-0/integration. WS-10 keeps the default-disabled runtime import-safe, but `SCHEDULER_ENABLED=true` raises a clear `RuntimeError` until this dependency is available.

## Upstream runtime contracts

The current worktree does not yet contain `src/core/events.py` or WS-8's frozen `run_pipeline` implementation. WS-10 expects:

- `src.core.events.subscribe(event_type: str, handler)` for `signal.raised` and `clock.advanced`.
- `src.agents.multi_agent.graph.run_pipeline(trigger, *, product_ids=None, run_id=None)` exactly as frozen in `15-SHARED-CONTRACTS.md`.

No local substitutes were added outside `src/runtime/`.